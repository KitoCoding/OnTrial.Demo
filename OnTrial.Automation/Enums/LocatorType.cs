namespace OnTrial.Automation
{
    /// <summary>
    /// The different ways we can search for an element on a page
    /// </summary>
    public enum LocatorType
    {
        Id, ClassName, CssSelector, LinkText, Name, PartialLinkText, TagName, XPath
    }
}
